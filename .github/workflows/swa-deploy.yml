# .github/workflows/swa-deploy.yml — CS01 deliverable #7 / CS01-9.
#
# Deploys the static frontend and .NET Function to Azure Static Web Apps.
# Push to main  → production deploy.
# Pull request  → preview environment (SWA action posts preview URL comment
#                 to the PR using GITHUB_TOKEN; requires pull-requests: write).
# PR closed     → preview environment torn down (close_pull_request job).
#
# IMPORTANT (CS01-9): this workflow is committed unguarded per design.
# Before gate G5 (user stores AZURE_STATIC_WEB_APPS_API_TOKEN as a secret)
# the deploy step will fail with a missing-secret error. That is expected and
# intentional — do NOT add an `if:` guard to skip when the secret is absent.

name: swa-deploy

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [main]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: swa-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    name: build-and-deploy
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332 # v4.1.7
        with:
          fetch-depth: 0
      - uses: Azure/static-web-apps-deploy@1a947af9992250f3bc2e68ad0754c0b0c11566c9 # v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "src"
          api_location: "api"
          output_location: ""

  close-pull-request:
    name: close-pull-request
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    steps:
      - uses: Azure/static-web-apps-deploy@1a947af9992250f3bc2e68ad0754c0b0c11566c9 # v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: "close"
